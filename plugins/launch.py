class bot:
    on='on'
    off='off'
    input.staticmethod.on.True
    input.staticmethod.off.False
class on:
    if on.long.True:
        poplio.io:
            on:
                server.open:
                    ./launch.py 
class off():
    if off.long.False:
        poplio.io:
            on:
                server.open:
usage = {
    !launch >>> for launch bot
return {
  on
  off
}
}
