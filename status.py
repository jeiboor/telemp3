{
status.number=some.from.number
from dy
  {
    text:member= for moderators only 
    text:text= function run (msg, matches)
  return={
    useage={status number 
      patterns={
        ^(title)(settings)$
        @echo off={you're admin
      {
    {
  {
{
  {
    end 
      end
{